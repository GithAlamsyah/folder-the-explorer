import * as foldersSchema from './folders';
// import other schemas here

// Combine all exports into a single 'combinedSchema' object
export const combinedSchema = {
    ...foldersSchema,
    // Add other schemas to export here
}